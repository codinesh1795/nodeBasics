import os from 'os';

// interact for memory or cpu etc base system

// userInfo
console.log(os.userInfo());

// totalmem()
console.log(os.totalmem());

// freemem()
console.log(os.freemem()); // in bytes 

// cpus()
console.log(os.cpus()); // each core wise